# Alias Manager

## Installation

```shell
lein cljsbuild once
```

This will generate am file. Add the am file to path.

## Usage
```shell
am -c # Clear db
am -n l ls # alias l => ls
am l # execute alias l
```

